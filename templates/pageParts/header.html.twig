<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid justify-content-between">
    <a class="navbar-brand" href="{{ url('home') }}">Home</a>
    
    <div class="navbar-collapse collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="{{ url('contact') }}">Kapcsolat</a>
        </li>
        {% if isLogged %}
            <li class="nav-item">
              <a class="nav-link" href="{{ url('messages_list') }}">Üzenetek</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ url('app_logout') }}">Kilépés</a>
            </li>
        {% else %}
            <li class="nav-item">
              <a class="nav-link" href="{{ url('form_login') }}">Bejelentkezés</a>
            </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>